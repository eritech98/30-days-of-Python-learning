#!usr/bin/env python3

#Find the length of the text python and convert the value to float and convert it to string


a = str("python")

b = len(a)

c = float(b)
d = str(c)
print("The length of python string is ",b,".","we then convert it to float , which becomes",c,"Here is the data type after conversion.",type(c),"We then convert the float",c,"\n","to string",d,".","Here is the data type after conversion.",type(d))


